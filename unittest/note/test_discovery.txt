Test Discovery: Test discovery is the process of discovering and executing all the
tests in the project directory and all its subdirectories.

It can be invoked with the following command:

python3 -m unittest discover
